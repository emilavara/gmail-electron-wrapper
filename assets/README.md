# Icons

Drop icon files in this folder:

- `icon.png` for macOS Dock (dev), Linux/window fallback, and notifications fallback
- `icon.icns` optional macOS Dock source (useful for packaged builds)
- `icon.ico` optional Windows window/taskbar icon
- `notification.png` optional dedicated notification icon
